function mouseClicked() {
    switch (gameState) {
        case 0:
            mStartScreen.mouseClickedStart();
            break;
        case 1:
            // mLobbyScreen.mouseClickedLobby();
            break;
        case 2:

            break;
        case 3:

            break;
    }
}